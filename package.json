{
  "name": "spices",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build --emptyOutDir",
    "dev": "vite",
    "init:hooks": "git config core.hooksPath .github/hooks && chmod +x .github/hooks/*",
    "init": "npm run init:hooks",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^5.4.10"
  }
}
